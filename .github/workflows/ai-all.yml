name: AI Workflows

on:
  workflow_dispatch:
    inputs:
      commit_sha:
        description: "Commit SHA to review (leave blank for latest main)"
        required: false
        type: string
  # STATUS: Disabled — automatic triggers replaced by workflow_dispatch-only until ai-workflows stabilizes
  # --- DISABLED AUTOMATIC TRIGGERS (restore by uncommenting) ---
  # pull_request:
  #   types: [opened, synchronize]
  # issue_comment:
  #   types: [created]
  # pull_request_review:
  #   types: [submitted]
  # check_suite:
  #   types: [completed]
  # push:
  #   branches: [main]
  # --- END DISABLED ---

permissions: {}

jobs:
  # STATUS: Disabled — post-merge re-dispatch depends on push trigger above
  # --- DISABLED AUTOMATIC TRIGGERS (restore by uncommenting) ---
  # # Push to main → re-dispatch as workflow_dispatch so post-merge suites
  # # can use the commit SHA via inputs.commit_sha (workflow_run context workaround).
  # post-merge-dispatch:
  #   if: github.event_name == 'push'
  #   runs-on: ubuntu-latest
  #   permissions:
  #     actions: write
  #   steps:
  #     - name: Re-trigger as workflow_dispatch
  #       env:
  #         WORKFLOW_NAME: ${{ github.workflow }}
  #         GH_TOKEN: ${{ github.token }}
  #       run: |
  #         gh workflow run "$WORKFLOW_NAME" \
  #           --repo "${{ github.repository }}" \
  #           --ref main \
  #           -f commit_sha="${{ github.sha }}"
  # --- END DISABLED ---

  all:
    # STATUS: Disabled — guard condition not needed without push trigger
    # --- DISABLED AUTOMATIC TRIGGERS (restore by uncommenting) ---
    # if: github.event_name != 'push'
    # --- END DISABLED ---
    permissions:
      actions: read
      checks: read
      contents: write
      id-token: write
      issues: write
      pull-requests: write
    uses: dryvist/ai-workflows/.github/workflows/suite-all.yml@v0
    with:
      caller_event: ${{ github.event_name }}
      review_prompt: "Focus on Nix flake patterns, home-manager module structure, AI tool configuration best practices"
      commit_sha: ${{ inputs.commit_sha || github.sha }}
    secrets: inherit
