name: Best Practices

on:
  workflow_dispatch:
  # --- DISABLED AUTOMATIC TRIGGERS (restore by uncommenting) ---
  # schedule:
  #   - cron: "0 3 * * 2,5"
  # --- END DISABLED ---

permissions:
  contents: read
  id-token: write
  issues: write
  pull-requests: read

concurrency:
  group: best-practices-${{ github.ref }}
  cancel-in-progress: true

jobs:
  best-practices:
    uses: JacobPEvans/ai-workflows/.github/workflows/best-practices.yml@v0.12.12
    secrets: inherit # zizmor: ignore[secrets-inherit]
