name: Code Simplifier
on:
  workflow_dispatch:
  # STATUS: Disabled — scheduled runs paused until code-simplifier workflow is validated
  # --- DISABLED AUTOMATIC TRIGGERS (restore by uncommenting) ---
  # schedule:
  #   - cron: "0 4 * * 0,3,6"
  # --- END DISABLED ---
permissions:
  contents: write
  id-token: write
  pull-requests: write

concurrency:
  group: code-simplifier
  cancel-in-progress: true

jobs:
  simplify:
    uses: dryvist/ai-workflows/.github/workflows/code-simplifier.yml@v0.16
    secrets: inherit # zizmor: ignore[secrets-inherit]
