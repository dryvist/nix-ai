name: Next Steps
on:
  workflow_dispatch:
  # --- DISABLED AUTOMATIC TRIGGERS (restore by uncommenting) ---
  # schedule:
  #   - cron: "0 5 * * 1,4"
  # --- END DISABLED ---
permissions:
  contents: write
  id-token: write
  pull-requests: write
  issues: write

concurrency:
  group: next-steps
  cancel-in-progress: true

jobs:
  next-steps:
    uses: JacobPEvans/ai-workflows/.github/workflows/next-steps.yml@v0.15
    secrets: inherit # zizmor: ignore[secrets-inherit]
