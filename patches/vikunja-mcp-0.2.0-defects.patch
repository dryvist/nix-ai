diff --color -ruN a/dist/tools/projects.js b/dist/tools/projects.js
--- a/dist/tools/projects.js	2026-09-22 16:38:13
+++ b/dist/tools/projects.js	2026-09-22 16:38:13
@@ -106,6 +106,7 @@
         page: zod_1.z.number().optional(),
         perPage: zod_1.z.number().optional(),
         search: zod_1.z.string().optional(),
+        fields: zod_1.z.array(zod_1.z.string()).optional(),
         // Share fields
         shareId: zod_1.z.number().optional(),
         shareHash: zod_1.z.string().optional(),
@@ -134,7 +135,20 @@
                             params.s = args.search;
                         if (args.isArchived !== undefined)
                             params.is_archived = args.isArchived;
-                        const projects = await client.projects.getProjects(params);
+                        let projects = await client.projects.getProjects(params);
+                        // Cut response size: return only the requested fields instead of
+                        // the full project object (list responses were reported as "huge").
+                        if (args.fields && args.fields.length > 0) {
+                            const fields = args.fields;
+                            projects = projects.map((project) => {
+                                const picked = {};
+                                for (const field of fields) {
+                                    if (field in project)
+                                        picked[field] = project[field];
+                                }
+                                return picked;
+                            });
+                        }
                         const response = (0, index_1.createStandardResponse)('list-projects', `Retrieved ${projects.length} project${projects.length !== 1 ? 's' : ''}`, { projects }, { count: projects.length, params });
                         return {
                             content: [
diff --color -ruN a/dist/tools/tasks/bulk-operations.js b/dist/tools/tasks/bulk-operations.js
--- a/dist/tools/tasks/bulk-operations.js	2026-09-22 16:38:13
+++ b/dist/tools/tasks/bulk-operations.js	2026-09-22 16:42:47
@@ -137,124 +137,14 @@
         const client = await (0, client_1.getVikunjaClient)();
         // Use the proper bulk update API endpoint
         try {
-            // Build the bulk update operation using TaskBulkOperation interface
-            const bulkOperation = {
-                task_ids: taskIds,
-                field: args.field,
-                value: args.value,
-            };
-            // Special handling for repeat_mode conversion
-            if (args.field === 'repeat_mode' && typeof args.value === 'string') {
-                const modeMap = {
-                    default: 0,
-                    month: 1,
-                    from_current: 2,
-                };
-                bulkOperation.value = modeMap[args.value] ?? args.value;
-            }
-            // Call the proper bulk update API
-            logger_1.logger.debug('Calling bulkUpdateTasks API', { bulkOperation });
-            const bulkUpdateResult = await client.tasks.bulkUpdateTasks(bulkOperation);
-            // Handle inconsistent return types from the bulk update API
-            // Sometimes it returns Message object, sometimes Task[] array
-            let updatedTasks = [];
-            let bulkUpdateSuccessful = false;
-            if (Array.isArray(bulkUpdateResult)) {
-                // API returned Task[] array - verify the updates were actually applied
-                if (bulkUpdateResult.length > 0) {
-                    bulkUpdateSuccessful = true;
-                    // Check if the returned tasks have the expected values
-                    for (const task of bulkUpdateResult) {
-                        switch (args.field) {
-                            case 'priority':
-                                if (task.priority !== args.value) {
-                                    logger_1.logger.warn('Bulk update API returned task with unchanged priority', {
-                                        taskId: task.id,
-                                        expectedPriority: args.value,
-                                        actualPriority: task.priority,
-                                    });
-                                    bulkUpdateSuccessful = false;
-                                }
-                                break;
-                            case 'done':
-                                if (task.done !== args.value) {
-                                    logger_1.logger.warn('Bulk update API returned task with unchanged done status', {
-                                        taskId: task.id,
-                                        expectedDone: args.value,
-                                        actualDone: task.done,
-                                    });
-                                    bulkUpdateSuccessful = false;
-                                }
-                                break;
-                            case 'due_date':
-                                if (task.due_date !== args.value) {
-                                    logger_1.logger.warn('Bulk update API returned task with unchanged due date', {
-                                        taskId: task.id,
-                                        expectedDueDate: args.value,
-                                        actualDueDate: task.due_date,
-                                    });
-                                    bulkUpdateSuccessful = false;
-                                }
-                                break;
-                            case 'project_id':
-                                if (task.project_id !== args.value) {
-                                    logger_1.logger.warn('Bulk update API returned task with unchanged project ID', {
-                                        taskId: task.id,
-                                        expectedProjectId: args.value,
-                                        actualProjectId: task.project_id,
-                                    });
-                                    bulkUpdateSuccessful = false;
-                                }
-                                break;
-                        }
-                        if (!bulkUpdateSuccessful)
-                            break;
-                    }
-                    if (bulkUpdateSuccessful) {
-                        updatedTasks = bulkUpdateResult;
-                    }
-                }
-            }
-            else if (bulkUpdateResult &&
-                typeof bulkUpdateResult === 'object' &&
-                'message' in bulkUpdateResult) {
-                // API returned Message object - treat as success but need to fetch updated tasks
-                logger_1.logger.debug('Bulk update API returned message object', { result: bulkUpdateResult });
-                bulkUpdateSuccessful = true;
-            }
-            if (!bulkUpdateSuccessful) {
-                // Bulk update didn't actually update the values, throw an error to trigger fallback
-                throw new Error('Bulk update API reported success but did not update task values');
-            }
-            // If we don't have the updated tasks yet (Message response), fetch them
-            if (updatedTasks.length === 0) {
-                const fetchResults = await processBatches(taskIds, constants_1.BULK_OPERATION_BATCH_SIZE, async (batch) => {
-                    const results = await Promise.allSettled(batch.map((id) => client.tasks.getTask(id)));
-                    return results;
-                });
-                updatedTasks = fetchResults
-                    .filter((result) => result.status === 'fulfilled')
-                    .map((result) => result.value);
-            }
-            const response = {
-                success: true,
-                operation: 'update',
-                message: `Successfully updated ${taskIds.length} tasks`,
-                tasks: updatedTasks,
-                metadata: {
-                    timestamp: new Date().toISOString(),
-                    count: taskIds.length,
-                    affectedFields: [args.field],
-                },
-            };
-            return {
-                content: [
-                    {
-                        type: 'text',
-                        text: JSON.stringify(response, null, 2),
-                    },
-                ],
-            };
+            // Task 3413 defect #6: Vikunja's server-side bulk endpoint does a
+            // full-object overwrite per task rather than a partial patch, so a
+            // `bulk-update field=X` request clobbered every other field to its
+            // zero value (description "", priority 0) while still reporting
+            // success. Never trust it -- always take the GET-merge-PUT
+            // fallback below, which updates one field on the task's own
+            // current state.
+            throw new Error('vikunja-mcp: bulk endpoint bypassed, using per-task GET-merge-PUT (task 3413 defect #6)');
         }
         catch (bulkError) {
             // If bulk update fails, fall back to individual updates
@@ -563,7 +453,15 @@
         // Create tasks in batches
         const projectId = args.projectId; // TypeScript knows this is defined due to earlier check
         const creationResults = await processBatches(args.tasks, constants_1.BULK_OPERATION_BATCH_SIZE, async (batch) => {
-            const results = await Promise.allSettled(batch.map(async (taskData) => {
+            // Sequential, not Promise.allSettled(batch.map(...)): concurrent creates race
+            // Vikunja's server-side per-project task index, so parallel batch members can
+            // collide on the same identifier (e.g. "#443" assigned to 10 of 12 tasks).
+            // Result shape (status/value/reason) is kept identical to allSettled's so the
+            // downstream filter/map code below is unchanged.
+            const results = [];
+            for (const taskData of batch) {
+                try {
+                    const value = await (async (taskData) => {
                 // Create the base task
                 const newTask = {
                     title: taskData.title,
@@ -636,7 +534,13 @@
                     }
                 }
                 return createdTask;
-            }));
+                    })(taskData);
+                    results.push({ status: 'fulfilled', value });
+                }
+                catch (reason) {
+                    results.push({ status: 'rejected', reason });
+                }
+            }
             return results;
         });
         // Process results
diff --color -ruN a/dist/tools/tasks/crud.js b/dist/tools/tasks/crud.js
--- a/dist/tools/tasks/crud.js	2026-09-22 16:38:13
+++ b/dist/tools/tasks/crud.js	2026-09-22 16:42:21
@@ -200,6 +200,13 @@
         if (!args.id) {
             throw new index_1.MCPError(index_1.ErrorCode.VALIDATION_ERROR, 'Task id is required for update operation');
         }
+        // Task 3413 defect #5 (corrected): `field`/`value` are bulk-update's
+        // params, not update's. `update` accepted and silently ignored them,
+        // returning success with nothing changed. Reject instead of a silent
+        // no-op -- use the named parameter (e.g. `done`) or `bulk-update`.
+        if (args.field !== undefined || args.value !== undefined) {
+            throw new index_1.MCPError(index_1.ErrorCode.VALIDATION_ERROR, "`update` does not support `field`/`value` (those are for `bulk-update`); use the named parameter instead, e.g. `done: true`.");
+        }
         (0, validation_1.validateId)(args.id, 'id');
         // Validate date if provided
         if (args.dueDate) {
@@ -237,11 +244,16 @@
             affectedFields.push('labels');
         if (args.assignees !== undefined)
             affectedFields.push('assignees');
+        if (args.projectId !== undefined && args.projectId !== currentTask.project_id)
+            affectedFields.push('projectId');
         // Build update object by merging current task data with updates
         // This prevents the API from clearing fields that aren't explicitly updated
         const updateData = {
             ...currentTask,
             // Override with any provided updates
+            // projectId was previously accepted by the schema but silently dropped here,
+            // so `update projectId=<n>` never moved the task.
+            ...(args.projectId !== undefined && { project_id: args.projectId }),
             ...(args.title !== undefined && { title: args.title }),
             ...(args.description !== undefined && { description: args.description }),
             ...(args.dueDate !== undefined && { due_date: args.dueDate }),
diff --color -ruN a/dist/tools/tasks/index.js b/dist/tools/tasks/index.js
--- a/dist/tools/tasks/index.js	2026-09-22 16:38:13
+++ b/dist/tools/tasks/index.js	2026-09-22 16:38:13
@@ -72,8 +72,11 @@
             tasks = await client.tasks.getProjectTasks(args.projectId, params);
         }
         else {
-            // Get all tasks across all projects
-            tasks = await client.tasks.getAllTasks(params);
+            // GET /tasks/all returns 500 on this server. Fetch per project and merge
+            // client-side instead of relying on the server's cross-project endpoint.
+            const projects = await client.projects.getProjects({});
+            const perProjectResults = await Promise.all(projects.map((project) => client.tasks.getProjectTasks(project.id, params)));
+            tasks = perProjectResults.flat();
         }
         // Apply client-side filtering if we have a filter expression
         if (filterExpression) {
